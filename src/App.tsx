import { ConfigProvider } from 'antd';
import './App.css'
import { BrowserRouter } from 'react-router-dom';

function App() {

  return (
    <ConfigProvider>
      <BrowserRouter>
        
      </BrowserRouter>
    </ConfigProvider>
  )
}

export default App
